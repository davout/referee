#!/usr/bin/env ruby

$:.unshift File.join(File.dirname(__FILE__), 'lib')   

require 'referee'

paymium = Referee::Exchanges::Paymium.new

EM.run do
  paymium.connect

  EM.add_periodic_timer(10) do
    puts paymium.market_info
  end
end
